# AI Code Intelligence Platform — Backend Dependencies
# -------------------------------------------------------
# Core API framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Data validation
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Environment variable support
python-dotenv>=1.0.0

# AST parsing (Tree-sitter v0.23+ uses the new Python bindings)
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
# Uncomment when needed:
# tree-sitter-typescript>=0.23.0
# tree-sitter-java>=0.23.0

# LLM providers (install only what you use)
openai>=1.54.0
anthropic>=0.39.0

# Dev / testing
httpx>=0.27.0       # For async test client with FastAPI TestClient
pytest>=8.3.0
pytest-asyncio>=0.24.0
